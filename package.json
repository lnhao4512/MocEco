{
  "name": "moc-ecopure-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd client-customer && npm install && cd ../server && npm install",
    "build-customer": "cd client-customer && npm run build",
    "start-server": "cd server && npm start",
    "build": "cd client-customer && cross-env CI=false npm run build"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cross-env": "^10.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.2",
    "react-router-dom": "^7.13.1",
    "react-scripts": "^5.0.1"
  }
}
