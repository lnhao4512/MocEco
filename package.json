{
  "name": "moc-ecopure-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client-customer",
    "client-admin",
    "server"
  ],
  "scripts": {
    "install-all": "npm install",
    "build": "cd client-customer && npm install && cross-env CI=false npm run build"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cross-env": "^10.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.2",
    "react-router-dom": "^7.13.1",
    "react-scripts": "^5.0.1"
  }
}
